<div class='input-group'>
  {{input type='file' accept='application/csv' value=inputCSV class='form-control'}}
  <span class="input-group-btn">
    <button
      type="submit"
      class="btn btn-primary"
      {{action 'uploadCSV'}}>
      Upload
    </button>
  </span>
</div>
